<h1 class="general">Categories of goals - choose what interests you most:</h1>
<ul><% @categories.each do |category| %>
<li><%= link_to category.name, category_path(category) %> contains <%= category.goals.count%> goals(s)</li>
<% end %></ul>